import { createIcon } from '../createIcon';
import {
  MeetingRoomIcon as HugeMeetingRoomIcon,
  TelegramIcon as HugeTelegramIcon,
  WhatsappIcon as HugeWhatsappIcon,
} from '@hugeicons/core-free-icons';

export const SiGooglemeet = createIcon(HugeMeetingRoomIcon);
export const SiTelegram = createIcon(HugeTelegramIcon);
export const SiWhatsapp = createIcon(HugeWhatsappIcon);
